#include<stdio.h>
#include<string.h>
void main()
{
    char str[100];
    printf("\n enter any string :");
    scanf("%s",str);
    strlen(str);
    printf("\n length of the string is : %d",strlen(str));

}